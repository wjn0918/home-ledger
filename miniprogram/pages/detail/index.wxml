<view>
  <view>当前家庭：{{currentFamilyName || '未选择'}}</view>
  <picker mode="selector" range="{{families}}" range-key="name" value="{{familyIndex}}" bindchange="onFamilyChange">
    <view>切换家庭</view>
  </picker>

  <block wx:for="{{groupedBills}}" wx:key="day">
    <view>日期：{{item.day}}（当日总金额：¥{{item.total}}）</view>
    <block wx:for="{{item.items}}" wx:key="id">
      <view>{{item.bill_date}} {{item.category}} ¥{{item.amount}} {{item.note}}</view>
    </block>
  </block>
</view>
